def project_status() -> dict[str, str]:
    """
    Tiny starter function so the repo has something testable on day one.
    Replace or expand this as the real project code is added.
    """
    return {
        "status": "ok",
        "project": "codeflow",
        "component": "ci",
    }
